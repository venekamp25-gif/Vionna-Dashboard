import { HomeDecorWorkbench } from "@/components/lighting/HomeDecorWorkbench";

/** Home Decor portal — import a lamp → write the copy → publish to The Light
 *  Supplier. Mirrors the fashion flow's shape, but built around how lamps
 *  actually work: one product with its own variants, specs instead of sizes. */
export default function HomeDecorPortal() {
  return <HomeDecorWorkbench />;
}
