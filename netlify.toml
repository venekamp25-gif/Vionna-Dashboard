# Netlify build configuration for Vionna Dashboard (Next.js in frontend/)
[build]
  base      = "frontend"
  command   = "npm run build"
  publish   = ".next"
  functions = ".netlify/functions-internal"

# Use Netlify's official Next.js plugin so API routes + SSR work.
[[plugins]]
  package = "@netlify/plugin-nextjs"
